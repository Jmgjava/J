package org.java.dao;

public class MemberDao {

	public int insert(int no, String userId, String userPw, String name, String number) {
		// 사용자 입력 값을 -> DB에 저장
		// 성공이면 1, 나머지는 실패
		// 1. DB 연동 JDBC (오라클, mysql)
		// 2.

		return 1;
	}

}
